version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      # Persist the instance folder where the SQLite database is stored
      - instance_data:/app/instance
    restart: unless-stopped

volumes:
  instance_data:
